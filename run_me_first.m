clear all; close all;clc

%% Written by Kristina P. Sinaga 
% Add folders to path.

addpath(pwd);

cd dataset/;
addpath(genpath(pwd));
cd ..;

cd evaluation/;
addpath(genpath(pwd));
cd ..;

cd tools/;
addpath(genpath(pwd));
cd ..;
